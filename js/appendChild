appendChild, 
如果新增的子元素已经存在于父元素中，则是将子元素移动到父元素的末尾位置


DOM性能优化，尽量减少对DOM的操作

对DOM操作会引起重绘和回流两种影响，回流更加消耗性能，我们要减少造成回流的操作

在渲染页面之前，会有以下几步
1、生成DOM TREE
2、加载css
3、生成RENDER TREE
4、通过CPU计算图像处理，渲染页面



1、使用字符串拼接
最终只变化一次

2、使用文档碎片容器
创建文档碎片容器，将需要新增的内容加入到文档碎片容器中，最终都处理好之后，将文档碎片容器加到相应父元素中去

这样只会引发一次回流操作

```javascript
let DomContainer = documnet.createDocumentFragment();
```
